---
target: restyled index.html + component index
total_score: 21
max_score: 40
na_heuristics: 
p0_count: 1
p1_count: 2
timestamp: 2026-09-09T03-24-26Z
slug: index-html
---
Method: dual-agent (A: ses_f7bd016bcffeUZ13UEeZLtJW5O · B: ses_f7bd016a7ffeYJ0fleokv0wlV1) · ⚠️ DEGRADED note: no browser available — live overlay skipped, detector ran in regex fallback (undercount, not a clean bill).

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | No busy indicator over 11k-row filter; dropdown badge shows 0 permanently |
| 2 | Match System / Real World | 3 | `$A`/`IDG` type codes + Unknown/Unknown Magic chips expose system, not world |
| 3 | User Control and Freedom | 2 | Reset wipes 7 groups with no undo; sort-dir has no visible control |
| 4 | Consistency and Standards | 2 | Sort select (3 sorts) vs headers (6 sorts) can disagree; `«»` vs `▲▼` glyphs |
| 5 | Error Prevention | 2 | Dual price sliders cross silently; dropdown inner-search hides options unannounced |
| 6 | Recognition Rather Than Recall | 2 | Active source/type picks live inside collapsed dropdowns; no applied-filter summary |
| 7 | Flexibility and Efficiency | 2 | No `/` shortcut, no recent search, no rarity-ordered default (saved lists out of scope per PRD) |
| 8 | Aesthetic and Minimalist Design | 2 | Spine shows all 7 groups always; 6-column table on 360px is maximalism |
| 9 | Error Recovery | 1 | Empty state + `No rules summary recorded` diagnose but prescribe nothing |
| 10 | Help and Documentation | 2 | Log-scale price mapping unexplained; one-line fix, not external docs |
| **Total** | | **21/40** | **Acceptable** |

## Design Specificity Verdict

**LLM assessment:** Visual language is product-specific (ledger tnum prices, rarity label+pattern bands, attunement-first filter, vellum/ember tokens, 5e.tools sheet link). Interaction architecture is generic catalogue: sticky e-commerce filter bar + sortable table + bottom-sheet PDP + numbered pagination would work unchanged for auto parts. Nothing expresses the two stated jobs (in-session one-handed <10s lookup vs desktop compare-across research). Verdict: product-specific skin, interchangeable skeleton.

**Deterministic scan:** `detect.mjs --json index.html` exit 2, 10 findings, all advisory: 8× `design-system-font-size` (14px at lines 63,124,165,179,194,202,228,347 — off the DESIGN.md ramp of 28/16/12/16) and 2× `design-system-color` (line 337 `#5c3d00`/`#efe3c2`, artifact band outside the 5 documented rarity bands). Component index: 7 findings (6× font-size incl. 13px/20px, 1× em-dash prose). Zero error-severity anywhere. Detector agrees with the review on drift points (type ramp, artifact band) and caught nothing the review missed beyond exact lines; systematic 14px repetition reads as doc lag, not 8 independent defects. No false positives asserted; DEGRADED regex fallback means undercount.

**Visual overlays:** none — no browser automation in this harness; no overlay claimed.

## Overall Impression

Token-faithful, scope-disciplined, shareable — but the in-session mobile job fails on chrome height + horizontal table pan, and a P0 data-vocabulary leak (`$C`, `IDG` as peer filter options) poisons trust in the whole filter model. Biggest opportunity: make the phone path search-first (collapse chrome, trim columns) and normalize type vocabulary at generation time.

## What's Working

1. Token fidelity (index.html:285-287,293-301): plate measure, forward step, label+pattern bands, ledger tnum, witchlight focus — honors DESIGN.md without fantasy-chrome drift.
2. PRD scope discipline: search + 5 filter dims + 50/page + URL state + sticky spine + sheet = requirements, no saved lists / extraction / gold-plating smuggled in.
3. Shareable state + status microcopy (679-709, 530-531): desktop research is linkable and screen-reader announced.

## Priority Issues

- **[P0] Type dropdown exposes raw data codes.** index.html:484 (`$,$A,$C,$G,AdvEq,IDG…` as peer types). Generator root cause: `translate_type` falls back to the raw code (11_generate_html.py:93). Fix: map the 10 unmapped codes to player-facing names (`$/$C`→Currency, `$A`→Art Object, `$G`→Gemstone, `AdvEq`→Adventuring Gear, `Ingred`→Ingredient, `LTG`/`TB`→Trade Goods, `MF`→Ranged Weapon, `IDG`→Other) and change the fallback to `Other` so no future code ever leaks raw. Suggested: /impeccable polish.
- **[P1] Sticky spine overwhelms the in-session job on mobile.** index.html:66-80,458-524: 7 groups sticky, taller than results. Fix (in-scope, not an extra filter): search + count stay sticky <700px; rest collapses behind one `Filters (N)` disclosure; desktop unchanged. Suggested: /impeccable polish.
- **[P1] 6-column table forces horizontal phone scroll.** index.html:253-258,538-543. Fix: <700px hide Source/Type/Attune columns (facts remain in sheet 561-565); keep 44px plate. Suggested: /impeccable polish.
- **[P2] Ember spent on non-live state; gray twin bands dilute rarity.** index.html:138-145 badge `All 0` always ember; 579-583 artifact/mundane/unknown/varies gray twins. Fix: badge quiet at 0; document artifact + unclassified bands in DESIGN.md (doc drift) or map to nearest band. Suggested: /impeccable polish.
- **[P3] Empty + description-less states end flat.** index.html:728,788. Fix: empty row gets wired Clear-price/Reset buttons; sheet fallback becomes guidance + 5e.tools pointer. Copy only, no new scope. Suggested: /impeccable polish.

## Persona Red Flags

- **Casey (desktop comparer):** sort schizophrenia — select offers 3 sorts (514-518), headers 6 (538-543); header-sorting by Source/Type/Attune desyncs the select (839) and direction resets silently (962).
- **Sam (in-session mobile DM):** spine height + 130px sliders + 10 rarity chips demand two hands; table pan loses the price column at the moment of need; sheet focus lands on Close (802), not the answer.
- **Alex (new player):** `Unknown / Unknown Magic / Varies` + `$A / IDG` + unexplained `log scale` (498) is insider jargon with no gloss; attunement chips imply multi-select but behave single-select (874-881).

## Minor Observations

- `results-note` "tap a row" (531) — "select a row" covers pointer users too.
- `filter-count` badge rendered at 0 (468,480) — hide/quiet at 0.
- Sheet `aria-labelledby` good (555); needs focus trap to earn `aria-modal`.
- `valToPos`/`posToVal` rounding (614-624) can strand numeric inputs off slider step.
- Payload correctly externalized (items-data.js, ~4.7MB single-line, 11,941 objects); 200ms debounce + 50-row render will jank low-end phones.

## Questions

Questions skipped: ticket flow — prioritization fixed by spec (P0/P1 first, 45-min cap), review gate owned by Horowitz.
