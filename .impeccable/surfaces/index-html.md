---
version: 1
slug: "index-html"
primary_target: "index.html"
related_targets: []
---

# Surface brief: shop page (index.html)

Scope: single static shop page at repo root (index.html), generated by scripts/11_generate_html.py, served via Cloudflare Pages at shop.mistystep.net. Visitor mode: Operate.

Audience: DMs and players. Jobs: (1) in-session mobile quick-lookup, one-handed, search-first; (2) desktop research between sessions.

Action/task: find an item's price and key rules facts in seconds across 11,941 rows. Proof/content: the catalogue itself — name + type + source + key rules text per row, rarity-ordered, attunement-flagged, price-sorted.

Constraints: debounced client-side full-text search; price-range slider + attunement toggle + rarity/type/source filters; sort by price/name/rarity; 50/page with reset-to-1; load weight well under ~5MB or split payload; shareable filtered views via URL query params (saved lists deferred); Carbon contrast table; generic-clean structure with fantasy confined to tokens.

Chosen direction (PROVISIONAL, user may override): The Screen — DM-screen tri-panel quick-reference world on Carbon structure. Dense tabbed reference panels; sticky filter bar as the screen's spine; tap-row detail sheet that opens in one committed gesture; selected rows step forward in brightness/weight, never boxed chrome; one flat accent owns live state only; every row shares one fixed invariant measure. Raises carried: The Plate Measure (folio), The Single Accent Law (metro), The One-Pull Sheet (cape), The Forward Step (cracktro). Competitive alternates held: rarity-band encoding (cyclorama), keyboard-first typed search dialogue (phosphor terminal). Roll: concept-seed --scope direction --mode operate, key ea808fdd, assigned index 3 of 7.

Memorable moment: the filter bar behaves like the spine of a DM screen — search lands first, panels answer instantly, a tapped row lifts its detail sheet in one pull.

Unresolved: exact post-hands-on filter refinements; payload split vs shrink; standing-exit canon (plain dense data-table grid) available on request.
