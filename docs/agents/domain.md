# Domain Docs

## Layout

Single-context. Look for:

- `CONTEXT.md` at repo root — project domain language, key concepts, conventions
- `docs/adr/` at repo root — architectural decision records

## Status

These files do not exist yet. Skills that depend on them should skip gracefully.

## Consumer Rules

- Read `CONTEXT.md` before making architectural changes to learn domain terminology
- Check `docs/adr/` for past decisions before proposing new approaches
- If `CONTEXT.md` is missing, proceed without it — do not create it unless asked
